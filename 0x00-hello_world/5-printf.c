#include <stdio.h>

/**
 * main-Entry level
 *
 * Description: print with a new line
 * Return: always reurn 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
		return (0);
}
