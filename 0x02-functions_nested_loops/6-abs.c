#include "main.h"

/**
 *  * _abs - computes the absolute value of a number
 *   * @c: takes c as a parameter
 *
 *    * Return: 0
 */
int _abs(int c)
{
	if (c >= 0)
		return (c);
	else
		return (-c);
}
