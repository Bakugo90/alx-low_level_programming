#include <stdio.h>
/**
 * main - prints the number of the arguments passed
 * @argc: the number of arguments passed
 * @argv: the array containing the passed arguments
 * Return: 0
 */
int main(int argc, char *argv[])
{
	printf("%d\n", argc);
	return (0);
}
